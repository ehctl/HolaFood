import { StyleSheet } from 'react-native'

export const style = StyleSheet.create({
    outterCircle: {
        padding: 5,
        aspectRatio: 1,
        borderRadius: 25,
        borderWidth: 2,
        justifyContent: 'center',
        alignItems: 'center'
    },
    innerCircle: {
        aspectRatio: 1,
        borderRadius: 25,
    }
})
